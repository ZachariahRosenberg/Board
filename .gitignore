# dependencies
node_modules/

# builds
dist/
build/
*.tsbuildinfo

# local dev data (dev daemon / tests use a temp or in-repo data dir)
.data/

# env
.env
.env.*
!.env.example

# local agent token for dogfooding (never commit — invariant 8)
.board-token

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/*
!.vscode/extensions.json
