{
  "name": "board",
  "private": true,
  "workspaces": [
    "server",
    "cli"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@types/bun": "^1.4.2"
  }
}
